name: bills_payroll_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  shared_preferences: ^2.1.1
  intl: ^0.18.1
  path_provider: ^2.0.14
  csv: ^5.0.0
  fl_chart: ^0.60.0 # optional: for dashboard charts
  flutter_launcher_icons: ^0.14.1

flutter_launcher_icons:  
  ios: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
